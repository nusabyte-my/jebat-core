# 2026-03-31 - Session Notes

## OpenClaw Installed

- Installed OpenClaw CLI `2026.3.28` under `/home/humm1ngb1rd/.local`
- Created host runtime at `~/.openclaw`
- Seeded runtime from `jebat-core/integrations/openclaw/`
- Copied JEBAT workspace bundle into `~/.openclaw/workspace`
- Generated a local gateway token in `~/.openclaw/.env`
- Disabled Telegram in the initial config until a real bot token is provided

## Runtime Status

- Gateway service installed as user systemd unit: `~/.config/systemd/user/openclaw-gateway.service`
- Gateway is running on `ws://127.0.0.1:18789`
- Dashboard is exposed locally at `http://127.0.0.1:18789/`
- Runtime health shows agents: `main`, `builder`, `security`, `research`
- Workspace skill `hermes-agent` is visible to the installed OpenClaw runtime

## Notes

- Manually fixed the user systemd unit to use `EnvironmentFile=%h/.openclaw/.env` instead of embedded secret values
- Reloaded and restarted `openclaw-gateway.service`
- `openclaw gateway status` no longer reports the embedded-token warning
- Ollama is not reachable at `http://127.0.0.1:11434`, so local Ollama fallbacks are not currently available
