# 2026-04-23

## Local AI Runtime

- Checked local llama.cpp configuration: WinGet install, build `8864`, Vulkan and CPU backends loaded.
- Confirmed Ollama is installed with `OLLAMA_MODELS=E:\Ollama\models`; existing local models include `gemma4:e2b`, `qwen3:8b`, Hermes variants, and cloud aliases.
- Downloaded GGUF models to `C:\Users\shaid\Desktop\models\llama.cpp`:
  - `gemma-4-E2B-it-Q8_0.gguf` (`4.63 GB`)
  - `Qwen-Qwen3.6-35B-A3B-IQ4_XS.gguf` (`17.44 GB`)
- Verified both files have valid `GGUF` headers.
- Gemma smoke test loaded through `llama-cli` and produced output; backend showed `Vulkan0 (Intel(R) Iris(R) Xe Graphics)` plus host memory.
- Repaired Hugging Face `hf` CLI by restoring missing `typer` package files, then pinned shared CLI package versions back to the existing environment expectations.
- Removed unused `aider-chat`, `diff-match-patch`, `grep-ast`, `Scripts\aider.exe`, and leftover partial `~ider*` package directories.
- Added llama.cpp helper scripts in `C:\Users\shaid\Desktop\models\llama.cpp`: `chat-gemma.ps1`, `chat-qwen.ps1`, `start-gemma-server.ps1`, `start-qwen-server.ps1`, and `README.md`.
- Qwen3.6 35B-A3B load test failed with `-ngl 999` due to Intel Vulkan out-of-device-memory; verified it loads with `-ngl 12` at `-c 2048`, leaving about 379 MiB free on `Vulkan0`.
