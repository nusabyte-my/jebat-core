# JEBAT Core Requirements
# Version: 3.0.0
# Stripped — only deps actually used by CLI + agent loop

# ── Core Framework ──
pydantic>=2.5.3
pydantic-settings>=2.1.0
pyyaml>=6.0.1
python-dotenv>=1.0.0

# ── AI & LLM (lazy-imported) ──
openai>=1.10.0
anthropic>=0.8.1

# ── Async & HTTP ──
httpx>=0.26.0
aiohttp>=3.9.1

# ── Database (optional — lazy) ──
sqlalchemy>=2.0.0
asyncpg>=0.29.0

# ── CLI & UI ──
rich>=13.7.0
prompt-toolkit>=3.0.43

# ── Utilities ──
structlog>=24.1.0

# ── Testing ──
pytest>=7.4.4
pytest-asyncio>=0.23.3

# ── Optional heavy deps (install separately if needed) ──
# pip install jebat[ml]    → numpy, pandas, scikit-learn, xgboost, lightgbm
# pip install jebat[nlp]   → sentence-transformers, spacy, nltk
# pip install jebat[server] → fastapi, uvicorn, websockets, prometheus-client, opentelemetry-api, opentelemetry-sdk
# pip install jebat[db]     → pymongo, redis, alembic