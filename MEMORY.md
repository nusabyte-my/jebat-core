# MEMORY.md

This file is the root memory index for the workspace.

## Current Operating Memory

- This repository should be treated as a JEBAT workspace with `jebat-core/` as the canonical operating center.
- Canonical startup begins from `jebat-core/BOOTSTRAP.md`.
- Codex sessions should also load `CODEX_PROFILE.md`.
- If duplicate docs exist at the root and in `jebat-core/`, prefer the `jebat-core/` copy unless the user explicitly directs otherwise.

## Active Projects

- **jebat-core** — canonical JEBAT operating center, VPS services, multi-agent model. Updated to upstream **v8.2.0** on 2026-07-14 (25 commits); reconciled local LLM provider changes (`llamacpp` + `tokenrouter`/`custom`), fixed upstream-shipped conflict markers in `.env.example` and `Dockerfile`.
- **ALAND (AlandFeasi)** - `C:\Users\shaid\Desktop\nusabyte\ALAND`. Property feasibility study platform. pnpm monorepo (packages/frontend React+Vite+Tailwind, packages/backend Express+tRPC+Drizzle :3001, packages/shared feasibility engine). Reference models: boom.online, land.tech. Active from 2026-05-17. **VPS (current): `187.127.204.89`** (domain `alandfeasi.tech`; **IPv4:22 fail2ban-DROPped from dev box -> ssh/deploy via IPv6 `root@2a02:4780:5e:a100::1`**, key `~/.ssh/id_ed25519-hostinger`), webroot `/var/www/ALAND/packages/frontend/dist`, backend `/var/www/ALAND/packages/backend/dist` (pm2 `aland-backend` :3001), DB PostgreSQL 16 :5432. nginx listens on BOTH 80/443 + `[::]:80/443` (backup `/root/alandfeasi.nginx.bak-20260813`; keep OUT of sites-enabled or `nginx -t` fails). **2026-08-13: HIGH-RISE calculator workbook-exact vs `FS- High Rise (Tower & Podium Car Park) - Version2.xlsx`** - Development (density/plot-ratio/building-area/podium) + GDV + GDC B1-B11 + SUMMARY + Results all editable + parity-locked; engine locked to the cent via `engine-lock.test.ts` (landed GDC 406,279,824.92 / profit -41,812,074.92; HR GDC 193,146,807 / profit 31,895,393.38 / 14.17%). SW cache trap: bump `alandfeasi-vN` in `public/sw.js` per deploy + hard refresh. Deploy: `./deploy_to_vps.ps1` (Windows/pwsh only - .sh unusable on this box). **Legacy host `72.62.254.65` / `aland.nusabyte.cloud` / pm2 `aland-api` DECOMMISSIONED 2026-08-13** (commit `c8195e1`). Trunk-based git: branch off `main`, conventional commits, PR into protected `main`; remote `github.com/nusabyte-my/ALAND-Production.git`. 73 frontend / 59 backend tests green. See `memory/2026-08-13.md`. Older note (2026-07-14): full-number formatting (no K/M/B abbreviations except `LiveSummary.tsx`), GDV/GDC table `whitespace-nowrap`, GitHub Actions disabled (private repo, no billing) - direct-to-`main` workflow. See `memory/2026-07-14.md`.
- **Erawan-QSys** — `C:\Users\shaid\Desktop\nusabyte\Erawan-QSys`. Queue system for massage operations (QPOS). Next.js 14 + NestJS + Prisma/PostgreSQL monorepo. Branch: `erawanwellness`. **PRODUCTION** server (active V2): Hostinger **IPv6 `2a02:4780:5e:20bd::1`** (host `erawanhq`, ssh `-6 -i ~/.ssh/id_ed25519-hostinger opsadmin@...`), domain `qpos.erawanwellness.com`. Legacy `72.62.254.65` NOT production (but IS the jumphost for V2 when direct IPv6:22 blocked); `72.60.42.163` refused. V2 checkout at `/opt/erawanQPOS-v2` (ports 4001/5501), nginx on 443, PM2: `erawan-api-v2` + `erawan-web-v2` (+ `erawan-ops-agent`). DB: Docker postgres:16 `127.0.0.1:5433` (`docker exec -i erawanqpos-postgres-1 psql -U erawan -d erawan_qsys`). Node on V2: `/usr/local/nvm/versions/node/v20.20.2/bin` (bash -lc + CI=true mandatory for pnpm). **2026-08-24→29 mega-sessions** (SW v61→v99, see `memory/2026-08-24.md` + `memory/2026-08-29.md` + `.dream-2026-08-29.md`): Gantt fit-zoom law; session-modal UX (Change Room/Stop Early as stacked z-9100 popups w/ capture-phase Escape); **tax invoice reform** — one receiptNo per SALE shared across method lines (migration 20260828120000 dropped Payment.receiptNo unique), RFD- prefix on refund, REFUND watermark on receipts; refunds override cancelled on ALL payment status pills; eVoucher (QR client+server, print thermal + email via POST /vouchers/:code/email); VIP purchase flow (thermal contract on register, terms email + A4 PDF w/ QR member card + tier table via POST /vip-cards/:cardNo/email, auto-send when email given) + visit flow (member thermal receipt auto-print on membership checkout); payments table rework (Customer/Service/Note/Time cols, method filter, search incl. name); manual entry TODAY-ONLY gate w/ blocking popup + 24h time input; settlement calendar popup + thermal settlement report w/ per-method tick checklist; booking page hides checked_in-with-all-started (#X7Q1GI); PinPadModal stale-PIN re-verify fix (clear on close + open guard) — the real "can't lock queue" bug; modal close buttons never gated on busy. **Deploy gotchas**: jumphost root@.65 → opsadmin@IPv6, base64-encoded scripts, `CI=true` pnpm (corepack prompts), chown -R opsadmin after root builds (root-owned files block git pull), .next double-rm, pm2 restart both when api/db changed. All user passwords: `erawan2026`.
- **evolvepayboost-web** — `C:\Users\shaid\Desktop\EvolvePlayBoost\evolvepayboost-web`. Game boosting e-commerce (Diablo/POE2 etc). React+Vite frontend + Express/TS backend (`server/`, port 5001, PostgreSQL `evolveplayboost` @localhost:5432). Payments: Stripe dual-mode + CHIP. **Mail (2026-08-21): `mail.evolveplayboost.com` = Stalwart v0.16.14 in Docker on 72.62.254.65 (host network; SMTP 25/465/587; admin API 8080; creds via `docker inspect stalwart-mail` env `STALWART_RECOVERY_ADMIN`). NOT Hostinger mail.** App SMTP: noreply@evolveplayboost.com (service account), `.env` port 465 OK (`server/lib/email.ts` auto `secure: port===465`). Scoped `mustMatchSender` exception lets noreply@ send From any @evolveplayboost.com address; outbound pinned IPv4 (sourceIp 72.62.254.65 + mx route v4Only) because SPF `v=spf1 mx -all` has no IPv6 coverage — IPv6 delivery hard-fails SPF → Gmail spam. Stalwart settings live in PostgreSQL; changes need `stalwart-cli create Action/ReloadSettings`. See `memory/2026-08-21.md`. **DIR TRAP (2026-09-01):** REAL repo = `evolvepayboost-web` (evolve-PAY; git remote nusabyte-my/evolvepayboost-web.git; PostgreSQL `evolveplayboost` @localhost:5432). `evolveplayboost-web` (evolve-PLAY) is a stale src/pages-only stub. ONE LETTER apart - never hand-type; derive programmatically (Test-Path .git).
- **Serambi Tiffin** — `E:/AI-Dev/nusabyte-project/serambi tiffin - web`. React 19 + Vite frontend, Express/TypeScript backend, PostgreSQL `serambi_tiffin`. Live on VPS `72.62.254.65` (PM2 `serambi-tiffin` id 4, nginx reverse proxy, serambitiffin.my). Upgraded 2026-09-17: WhatsApp AI Sales Desk & Infoline Dispatcher (Groq Llama-3.3-70b / Gemini / Domain RAG fallback), Inbound 1-Click WhatsApp OTP + desktop QR Code + Mailcow Email OTP, Mailcow Suite & SOGo Webmail (mail.serambitiffin.my), dynamic RAG engine with 4-week menu roster, 9 delivery zones, and operational SLAs.
- **SkillPro** — `C:\Users\shaid\Desktop\nusabyte\SkillPro`. Domain `skillpro.my`. Malaysian freelancer/professional identity platform (v0-bootstrapped). Next.js 16 (App Router, port 4050) + React 19 + Tailwind 4 + shadcn + PostgreSQL (`@vercel/postgres` + `pg`) + NextAuth v5 (beta) + Drizzle-less raw SQL migrations in `db/migrations/` + Vitest. Repo: `github.com/nusabyte-my/SkillPro-WEB` (origin/main clean). Active phases: VPS deployment to 72.62.254.65, escrow + KYC modules already scaffolded under `lib/actions` and `lib/repositories`. See `REFACTOR_PLAN.md` and `DEPLOYMENT_GROWTH_PLAN.md`. Brand: green `#22C55E` + blue `#2563EB` + navy `#0F172A`.
- **Aether Energy** — `C:\Users\shaid\Desktop\Aether-Energy\Aether-energy-web`. AI-powered no-code oil trading platform for institutional traders. React 19 + Vite 7 + Tailwind CSS 4 + Express/TypeScript. pnpm monorepo (client/server/shared). Radix UI, framer-motion, recharts, wouter, zod. Design: "Elemental Precision" — dark slate with amber/gold accents. Registered 2026-06-15.
- **KenariCekal_Workforce** — `C:\Users\shaid\Desktop\nusabyte\KenariCekal_Workforce`. WorkforceOS — enterprise workforce attendance for Malaysian manpower agency (~200 staff, 24/7, two shifts/day, multiple sites). Phone-only GPS geofence (BLE/NFC/WiFi intentionally removed — do not reintroduce). NestJS 10 + TypeORM + PostgreSQL + Redis (backend :3000) / Vanilla JS PWA (served from NestJS, face enrollment + selfie liveness) / Next.js 16.2.9 + AntD 6 + Zustand + Leaflet (dashboard :5580). Phase 1 GPS MVP complete; Phase 2 pilot hardening not started. Build: backend typecheck clean, backend lint broken (ESLint 9 vs legacy .eslintrc.js), dashboard has 34 tsc errors. See `IMPROVEMENT_PLAN.md` for roadmap. Captured 2026-07-06.

## Session Notes

- 2026-09-08: **Erawan-QSys guest-scoped Cancel implemented** (`apps/web/app/(frontdesk)/booking/page.tsx`, working tree, NOT committed). Multi-guest bookings previously had only booking-level Cancel (`updateStatus("cancelled")` kills all guests). Now: guest row gets own `Cancel` button → `openCancelBooking(b.id, pendingItems.map(i => i.id))` → `confirmCancelBooking` DELETEs each of that guest's items via existing `removeItem` endpoint (`DELETE /bookings/:id/items/:itemId`) — reuse pattern from `confirmRemoveSession`. Last-item guard: `removeItem` throws `BadRequestException("Cannot remove the last item...")` when guest owns booking's only item → caught (succeeded===0) → fallback `updateStatus("cancelled")`. Modal copy switches on `cancelItemIds.length`. POS `removeGuestItems` already guest-scoped (per-item DELETE/PATCH + last-item→booking-cancel) — untouched. Gantt has no cancel surface. Build `pnpm --filter @erawan/web build` ✅. Key insight: `removeItem` side effects ≈ per-item portion of `updateStatus("cancelled")` (frees room/masseur pins, audit) — option-b reuse beat duplicating logic.
- 2026-09-09: **VPS Topology & JEBAT Services Restored (.206 Mainframe + .65 Ingress Gateway)**: Confirmed user directive that JEBAT VPS execution host is `72.62.255.206`. Rebuilt broken `.venv` on `.206` (incompatible Python 3.14 shebangs) with native Python 3.12.3 and full production dependencies (`fastapi`, `uvicorn`, `pydantic`, `starlette`, etc.). Started and persisted `jebat-api` (:8000) and `jebat-webui` (:8787) under PM2 on `72.62.255.206`. Enabled `/etc/nginx/sites-available/jebat` in `sites-enabled` and reloaded Nginx on `.206`. Verified `jebat-upstream-tunnel.service` on `.65` forwarding ports 8000 & 8787 to `.206`. Verified public endpoints `https://jebat.online/api/v1/health` (200 OK) and `https://jebat.online/webui/` (200 OK). Added `72.62.255.206` (`jebat-vps`) to `~/.ssh/config`.
- 2026-09-09: **AGI Cognitive Core & Multi-Domain Harness (Build × Design × UI/UX × Copywriting)**: Upgraded JEBAT MCP into a full sovereign cognitive harness. (1) Created `jebat/core/agi_core.py` with `AGICognitiveEngine`: grounds perception across SelfLearn facts, autoMimpi heuristics, and design tokens, and enforces autonomous Reflexion Gates across Build (AST syntax), Hallmark UI/UX (6-axis anti-slop, roman headers, 8-state coverage), and Pawang Jualan (generic CTA transformer & buzzword stripper). (2) Implemented Voyager-style dynamic tool synthesis (`jebat/tools/dynamic_synthesis.py` via `tool_synthesize`), persisting runtime tools to `~/.jebat/synthesized_tools/`. (3) Added `agi_execute` meta-tool to `jebat/tools/agent_tools.py`. (4) Added 6 design & copywriting MCP tools (`design_preflight`, `ui_critique`, `component_states`, `copy_audit`, `copy_transform_cta`, `copy_framework`) and 2 dynamic resources (`jebat://design/tokens`, `jebat://copy/rules`). Total active tools on MCP: **85 tools**. All 18 regression and AGI unit tests passed locally and on Linux VPS (`72.62.255.206`); PM2 services reloaded and live.
- 2026-09-09: **Autonomous Sleep Daemon, Live Native Streaming & WebUI AGI Audit**: (1) Activated `jebat-automimpi.service` and `jebat-automimpi.timer` on `.206` (runs daily at 04:00 MYT / 20:00 UTC) for autonomous memory pruning, trace consolidation, and pattern extraction. (2) Added native SSE chunk streaming to `LlamaCppProvider.generate_stream()` and `provider_openai.complete_stream()` with real-time border-wrapped token display in `TerminalUX`. (3) Added `/webui/audit` and `/webui/api/audit` in `jebat/services/webui/webui_server.py`, returning Hallmark 6-axis scores, 8-state interaction coverage, and Pawang Jualan copywriting audit with specific CTA transformations live over `https://jebat.online`.
- Daily session records live in `memory/YYYY-MM-DD.md`.
- Dream consolidations live in `memory/.dream-YYYY-MM-DD.md`.
- 2026-08-29: Local JEBAT regression cleanup completed. Restored `jebat/config/manager.py`, kept `tests/` intact, fixed EnhancedMemory Ghost indexing/deduplication/persistence and unhashable linked-result handling, and made token budgeting conservative across model/fallback estimators. Verification: `146 passed, 5 skipped`; frontend `127.0.0.1:8080` and backend `127.0.0.1:8000/health` both return HTTP 200. Existing untracked `.omo/`, assets, branding files, and restored manager remain preserved for review.
- 2026-08-29: MCP upgrade continued. Server now negotiates `2024-11-05`, `2025-03-26`, and `2025-06-18`; exposes `jebat://workflow`, `jebat://tools`, and `plan-act-verify-remember`; returns tool safety annotations and machine-readable approval metadata; streamable-http entrypoint is wired. Landing page now shows the governed intent→context→policy→execute→remember walkthrough. Verification: `150 passed, 5 skipped`; ruff clean on changed Python; local landing workflow passed at 375px and 1280px with no console errors; Windows MCP config output fixed to UTF-8.
- 2026-08-29: Published the updated landing page to `jebat.online` via targeted SCP after the legacy Windows auth-helper path failed because its native `pexpect` module lacked `spawn`; direct Hostinger SSH key path succeeded. Backed up `/var/www/jebat.online/index.html` as `index.html.bak-20260829-mcp`. Live HTTP returned 200; Playwright live verification passed at 375px and 1280px with MCP workflow interaction and no console errors.
- 2026-08-29: MCP landing section follow-up fixed the workflow block nesting so it renders as a full-width governed pipeline below the install panel; clarified copy around local MCP plus approval boundaries; changed install default to the truthful Python stdio command; renamed server-card status from ACTIVE to AVAILABLE. Re-published to `jebat.online` with backup `index.html.bak-20260829-mcp-section`; live HTTP 200 and responsive checks passed at 375px, 768px, and 1280px with no horizontal overflow.
- 2026-08-29: Reconciled production MCP runtime on `.206` using the actual `/root/jebat-core` checkout (not the stale `/var/www/jebat-core` copy). Rebuilt `jebat-mcp` and verified the live Streamable HTTP initialize handshake returns MCP `2025-03-26`. Fixed the compose healthcheck to use a finite TCP readiness probe because `GET /mcp` is intentionally long-lived SSE; container is healthy. Backups stored under `/root/jebat-core/.deploy-backups/20260829-mcp-runtime` and `docker-compose.mcp.yml.20260829-mcp-healthcheck`.
- 2026-08-29: CLI revamp baseline: `pyproject.toml` ships `jebat_cli_new.jebat:main` as the canonical `jebat` entrypoint; the older `jebat.cli.__main__` is a separate modular surface. Added a shared-registry bridge so the canonical CLI agent discovers and executes the 90 currently registered JEBAT tools, with shared safety classification and machine-readable JSON results. Also fixed legacy Windows cp1252 help crashes, aligned the CLI banner version to package `8.2.1`, removed import-time PendekarGit stdout noise, and wired `code` and one-shot `chat` provider/model overrides through the canonical agent. Verification: focused CLI/tool/MCP tests passed and full suite is `159 passed, 5 skipped`.
- 2026-08-30: Erawan QPOS POS header cleanup: the duplicate VIP, eVoucher, and Reporting controls live together in `apps/web/app/(frontdesk)/pos/page.tsx`. Removing those controls must preserve VIP/eVoucher modal state because the global navigation still opens them through `/pos?vip=1` and `/pos?voucher=1`; only `BarChart3` became unused. After running `next build` while a dev server is active, restart that server before visual QA because replacing `.next` can temporarily make its CSS/JS chunk URLs return 404. Web typecheck/build and 320/375/414/768px checks passed for this change.
- 2026-09-02: Synced Erawan QPOS production PostgreSQL from the active Hostinger V2 server to local Docker (`erawan-qsys-postgres-1`, localhost:5434). Created a reversible local pre-sync dump and a production custom-format dump under `.pg_tmp/db-sync-20260902-vps-to-local/`; retained the server-side production snapshot under `/home/opsadmin/backups/`. Post-restore verification matched production exactly for the 40-migration hash and key operational row counts; Prisma reports the local schema is up to date.
- 2026-09-04: Erawan QPOS local booking hardening now persists manual `BookingItem.subRoomId` pins and rejects overlapping sub-room reservations against assignments, pending booking pins, and sibling items in the same create request. Duration checks include `extraMinutes`; create uses Serializable isolation with bounded P2034 retry. Receipt reprint work also preserves the persisted tax invoice number and real split-tender lines without inventing card PAN tails. Local verification: API 123/123, monorepo typecheck, API build, and web build passed; changes remain uncommitted/undeployed.

- 2026-09-04: **Concrete-room selector invariant** — All room selectors across the QPOS frontend (session-modifier, pay-and-add-sheet, booking-form, room page) must display uppercase concrete sub-room options (`A1A`, `A1B`, `A1C`, `B2A`, `B2B`). The `subRoomId` field is preserved end-to-end through `FormSession` → backend `ChangeRoomDto` → `Assignment.subRoomId` → `Assignment.slotLabel`. Downstream: `SlotLights` uses `slotLabel` for red/green occupancy lights; `Gantt` block colors use black/brown/yellow/red/blue per the room-status display spec. Lowercase fallback `String.fromCharCode(97 + i)` was corrected to `65 + i` (uppercase 'A') in both `session-modifier.tsx` (line 413) and `booking-form.tsx` (line 1271). The `subRoomId` forward flow: `handleChangeRoom` in `room/page.tsx` now includes `assignment?.subRoomId` in the PATCH body; `ChangeRoomDto.subRoomId` is optional but forwarded from controller to service; `Assignment.freeSubRoom` validates the provided sub-room belongs to the target room before assignment. Inventory guard: `freeSubRoom` rejects a provided `subRoomId` if it's already busy in the requested window, otherwise falls back to the existing `freeSubRoom` logic. All 123 API tests, 129 core tests, typecheck, and builds pass.

- 2026-09-09: **Erawan-QSys booking UI + per-guest paid-state batch** (working tree, NOT committed; repo path now `E:\AI-Dev\nusabyte-project\Erawan-QSys`). Shipped: (1) Booking table dedicated **Phone Number** + **Notes** columns; sub-guest rows indented with `CornerDownRight` elbow + pl-5 under main guest (first-guest rows show booking id + customer). (2) New-booking customer picker: searchable combobox (name/phone, ≤10 hits), inline **Edit/Save** on selected-customer card (`PATCH /customers/:id`), quick-add row. Verified DB round-trip on local Docker `erawan-qsys-postgres-1` (port **5434** — NOT 5433; prod container is `erawanqpos-postgres-1:5433`, local is `erawan-qsys-postgres-1:5434`). (3) **Per-guest Gantt paid-state**: new `paymentForGuest(booking, guestIndex)` in `apps/api/src/live-matrix/live-matrix.service.ts` — matches `Payment.guestIndex` (stamped by multi-guest checkout since 2026-08-16), falls back to all-null-guestIndex completed payments for legacy single-guest rows, else unpaid. Root cause it fixes: booking-level `payments[0]` marked ALL guest blocks paid when ONE guest paid. (4) **Brown-block pay gate**: `upcoming-booking-modal.tsx` — unassigned blocks ALWAYS show "Pay & Start" (→ PayAndAddSheet → cart → yellow → POS payment → red); direct "Start Session" only for assigned AND guest-paid. (5) Separate **Phone** section under Customer in both `upcoming-booking-modal.tsx` and `session-modifier.tsx`. (6) **eVoucher detail modal** (`apps/web/components/voucher-detail-modal.tsx` + `GET /vouchers/:id/history`): row click opens holder card with balance/validity, code show/hide toggle, QR (`qrcode.toDataURL`), email resend (pre-filled from holder email), purchases (VoucherTopup) vs transactions-used ledger (payments matched via `VCH-<code>` reference — stamped on Payment rows at capture in `payments.service.ts` createCheckout/create/checkpoint + multi-guest). (7) **Invoice redaction** in `payments-modal.tsx`: refunded/voided capture lines show `•••••` (number must not re-enter circulation); the refund line (`id::refund`) shows the number STRIKETHROUGH so FD sees which invoice was refunded; completed invoices show numbers. (8) `.scrollbar-thin` CSS utility (globals.css, hairline transparent bar, thumb on hover) wired to booking-form guest list. All typechecks pass (full monorepo + api + web). Dev gotchas: `nest build` while `nest start --watch` runs kills port 4010 — restart with `pnpm --filter @erawan/api dev`. Dev API runs from `pnpm --filter @erawan/api dev` (script is `dev`, not `start:dev`). EAK-0000682/683 used as live fixtures for redaction verification (30D ledger window needed).

## Durable Decisions

- Architecture and operating decisions live in `jebat-core/vault/decisions/`.
- See `jebat-core/vault/decisions/2026-04-08-jebatcore-canonical-startup.md` for the accepted JEBATCore startup rule.
- See `jebat-core/vault/decisions/2026-04-16-llamacpp-jebat-llm-production-cutover.md` for the production `llama.cpp` cutover, VPS tuning, JEBAT chat preset routing, and the current `.65 -> .206` remote model-host topology.

## Integration Stack (MCP + Skills)

- Full stack map + configs: `vault/integrations/mcp-skill-stack.md` (captured 2026-08-10).
- **opencode** `~/.config/opencode/opencode.json`: `context7`, `sequential-thinking`, `skills` (FastMCP 4 Skills Provider — venv `~/.config/opencode/venvs/skills-mcp`, server `mcps/skills-mcp/server.py`, 120 skills as `skill://` resources). Backup: `opencode.json.bak.stack-20260810`.
- **jebat runtime** `~/.jebat/config.yaml`: same 3 servers under `mcp:` (context7 needs `Accept: application/json, text/event-stream`; sequential-thinking uses `npx.cmd` + 60s cold-start timeout). Backup: `config.yaml.bak-20260810`. **Live-verified 2026-08-10** via jebat MCPClient: context7=2 tools, sequential-thinking=1 tool, skills=144 resources.
- **Fixes shipped 2026-08-10:** jebat CLI v7.5 crashed on Windows (`UnicodeEncodeError` cp1252 banner) — patched `D:\Jebat\jebat-core\jebat_cli_new\jebat.py` (stdio UTF-8 reconfigure at import); `jebat mcp serve` handshake verified. opencode `jebat` MCP entry now sets `PYTHONUTF8=1` env.
- `worldmonitor` added 2026-08-10, removed same day (user request). `skills` was initially the anthropics `npx skills` CLI, swapped for the gofastmcp FastMCP Skills Provider per user link.
- Reality check: cc-switch = desktop GUI (not MCP), lobehub = MCP client (not server), awesome-mcp-servers + awesome-claude-skills = catalogs (sources, not installs).

## Current Production LLM Topology

- `72.62.254.65` is the public-facing JEBAT node for `jebat.online`.
- `72.62.255.206` is the stronger active `llama.cpp` model host for JEBAT chat.
- The live JEBAT stack on `.65` routes `LLAMA_CPP_HOST` to `.206` instead of using a local `.65` model process.
- `.206` exposes TCP `8081` only to `.65` for this path.
- The `.65 -> .206` remote `llama.cpp` route has been verified through the live OpenAI-compatible JEBAT chat endpoint with `provider: "llamacpp"`.

## 2026-08-30 — Till settlement backfill + direct-payment attribution (Erawan QPOS, fix b89e7df)

**Bug**: VIP/voucher top-ups (no booking/assignment, branch on Payment row itself) were classified `unlinked` by `till-accounting.ts` — `PAYMENT_SELECT` omitted `branchId`/`businessDate`, blocking automatic finalization. Fix: select both columns; relationless + fully direct-owned → attribute; null ownership → stays `unlinked`; other-branch → skipped silently. Red/green test added (till-service 15/15).

**Historical data resolution (2026-07-28, 9 null-branch bookings, 11 payments RM ~1.7k)**: attributed ALL to **nu-sentral**. Evidence: all 12 sessionCodes ENU-; masseurs queued at nu-sentral that day (zero AK-exclusive masseurs on those sessions); 2 assignments explicitly nu-sentral. **Room branch = noise pre-branch-scoping** (29 NU bookings used AK rooms same day) — never trust room branch alone for legacy attribution. Remaining null-branch bookings in window: 4 cancelled, zero payments (inert).

**Backfill procedure (verified, reusable)**: standalone JS runner placed in prod `apps/api/dist/till/` using compiled `TillService` (exact accounting logic, no SQL drift). Idempotent: upsert key `automatic_final:<branch>:<date>`; snapshot remediation via `UPDATE ... WHERE receiptJson IS NULL` (NOT a new row — DB CHECK `chk_till_settlement_type` only allows `manual`/`automatic_final`). 66/66 branch-dates in 2026-07-28..08-29 now have exactly one automatic_final with snapshot; pre-08-24 dates got synthetic sessions opening=0. Verification: cross-math SQL (booking-owned + direct-owned = snapshot total, exact on 08-25 AK: 743040+1000000=1743040).

**Gotchas recorded**:
- `TillSettlement` has NO `accumulatedTotalSalesCents` column — only cash variants; totals live in `receiptJson` snapshot only.
- pm2 restart as root needs `sudo -H -u root bash -lc "...nvm...pm2"` — plain `env PATH=$PATH` expands wrong under non-interactive SSH.
- Backfill scripts: keep OUT of `src/` (nest build rejects raw .js in TS rootDir); ship via scp to dist/, delete after.
- Automatic finalization cron is DISABLED by client request (2026-08) — manual settlement required; backfill was pre-authorized exception.
- Backups: `.pg_tmp/till_backfill_20260830/` local + `/home/opsadmin/backups/` prod + container /tmp.

## 2026-09-03 - Dev-workflow build hazards + DI import-type trap (Erawan QPOS, release: v2 settlement + VIP/eVoucher pages)

**Stale-`.next` corruption**: running `pnpm --filter @erawan/web build` while `next dev --port 5500` is still running overwrites `apps/web/.next` — the dev server keeps SSR-ing page HTML but the browser gets `404` on every `_next/static/*` chunk (`layout.css`, `main-app.js`, per-route chunks), so pages render but all interactivity is dead. Recovery: kill the PID listening on :5500 (`Get-NetTCPConnection -LocalPort 5500 -State Listen`), `Remove-Item apps/web\.next -Recurse -Force`, relaunch `pnpm --filter @erawan/web dev`. Rule: always stop the dev server before any production build of the same workspace.

**Local API dies the same way**: `nest build` overwrites `apps/api/dist/` while `nest start --watch` runs, killing the watcher silently (port 4010 stops answering with no error surfaced). Local API config lives in `apps/api/.env` (`PORT=4010` — NOT 4000 as README states). Restart via `Start-Process cmd.exe "/c pnpm --filter @erawan/api dev"` — plain `Start-Process pnpm` fails with "%1 is not a valid Win32 application" because pnpm is a shell script, not an exe.

**Prod incident + gate**: Biome `lint/style/useImportType --write` converted Nest DI-injected services (`PrismaService`, `TillService`, `MailService`, `ReceiptNumberService`, and both controllers) to `import type`, erasing the runtime imports → `Nest can't resolve dependencies` crash-loop in production, invisible to vitest (tests import services directly and never bootstrap Nest DI). Rules: (1) never blanket-apply `useImportType` to Nest provider/controller files, (2) verify API changes with a real `NestFactory.createApplicationContext` bootstrap check, not unit tests alone.

## 2026-09-03 - Payment integrity + till settlement reconciliation (Erawan QPOS, fix b8436bc→HEAD)

**Problem**: Repeated checkout of the same booking allocated fresh tax-invoice numbers each time (EAK-0000538/539/540 on one booking) and no remaining-balance guard → overcapture (#HNUFA7, 34420¢ excess). Settlement totals diverged from Transaction List because: (a) split payments summed per-method incorrectly, (b) refunded payments subtracted from `accumulatedTotalSalesCents` but not from `salesByMethod[cash]`, (c) ledger keyed by `createdAt` not `businessDate`, (d) manual/backdated/bulk settlements bypassed canonical accounting in `till-accounting.ts`.

**Three-layer payment fix (PaymentsService)**:
1. **Transaction-scoped balance enforcement** — new `assertBookingPaymentWithinBalance(tx, bookingId, branchId, incomingCents)` computes gross from items minus discounts/rounding, subtracts captured/pending/refunded net cents (via `paymentNetContribution` logic), rejects incoming > remaining INSIDE the Serializable transaction. Called from `createCheckout` (all paths), `createSplitCheckout`, `createMultiGuestCheckout`.
2. **Receipt reuse** — `existingServiceReceiptNo(tx, bookingId)` queries for first non-null service `receiptNo` on the booking; if found, reuses it instead of `receipts.next(...)`. Guarded by the same tx so retries don't leak sequence numbers.
3. **Serializable isolation on all checkout paths** — `prisma.$transaction(fn, { isolationLevel: "Serializable" })` on multi-method, split, multi-guest, and single internal checkouts. Bounded P2034 retry (3 attempts) already present.

**Till settlement fix (till-accounting.ts + TillService)**:
- `aggregateTillPayments` now correctly subtracts refunded net from both `byMethodCents` and `totalCents` (completed adds, refunded subtracts net, cancelled ignores).
- `listUnsettledDates` keys by `businessDate` (MYT), not `createdAt`, so payments on 2026-08-23 business date are correctly surfaced even if `createdAt` straddles UTC midnight.
- `settleBulk` now calls `settle` which uses `computeBranchSettlement` (canonical) instead of its own ad-hoc cashSalesCents calculation — backdated settlements match manual + automatic totals.
- Payment integrity issues (`unlinked`, `unattributed_owner`, `refunded_nonzero_net`, `relation_mismatch`) block `automaticFinalEligible` — operator must resolve before final close.

**Receipt formatting** — `formatRM(cents)` in `apps/web/lib/api.ts` now uses `toLocaleString("en-MY", {minimumFractionDigits:2, maximumFractionDigits:2, useGrouping:true})` → `RM999,999.00` everywhere (Transaction List headline, method breakdown, row totals, receipt view).

**Tests added** (15 payments-transactional + 19 till-service = 34 new regression tests):
- `createCheckout: rejects a payment that exceeds the booking's remaining balance`
- `createCheckout: reuses the booking's existing service invoice across checkout events`
- `createCheckout: uses Serializable isolation for booking balance enforcement`
- `aggregates split-payment methods correctly per checkout`
- `refund adjustments subtract from method totals and accumulated cash`
- `settlement keys by businessDate not payment createdAt`
- `bulk settle idempotent — each date settled once via settleBulk`

**Verification gates passed**:
- `pnpm --filter @erawan/api test` → 112/112 green
- `pnpm --filter @erawan/core test` → 129/129 green
- `pnpm typecheck` → clean across 5 workspaces
- `pnpm --filter @erawan/web build` → Next.js production build successful
- All dirty-tree files validated without production deployment

## 2026-09-06 - Sequential multi-guest POS checkout isolation (Erawan QPOS, local)

- Root cause: the POS can check out a single guest through generic `/payments/checkout`, but that contract previously discarded `guestIndex`, leaving new Payment rows unscoped (`NULL`). Consolidated cart cleanup also removed rows by bare `guestIndex`, which collides when one cart contains multiple bookings whose guest numbering restarts at zero.
- Fix pattern: carry optional `guestIndex` through CheckoutDto and every generic checkout Payment create path; validate the guest exists on the persisted booking inside the same Serializable balance-check transaction. The POS sends it only when the booking bucket contains exactly one guest.
- Cleanup invariant: remove successful cart rows by exact `CartItem.id`, never by `guestIndex` alone. Continue marking paid Gantt state by exact `bookingItemId`.
- Regression: two sequential checkout calls on one checked-in booking (guest 0 then guest 1) must both succeed and persist Payment.guestIndex 0/1 respectively. Local verification: API 128/128, monorepo typecheck clean, LSP diagnostics clean. Web production build intentionally skipped because a Next dev server was listening on port 5500.

## 2026-09-07 - Erawan QPOS V2 PM2 web recovery

- PM2 must not launch `apps/web/node_modules/.bin/next` as JavaScript; that file is a shell wrapper and crash-loops with a syntax error. Launch Node with `node_modules/next/dist/bin/next start -p 5501` instead.
- Next resolves `.next` from its working directory. For V2, `erawan-web` must use `cwd=/opt/erawanQPOS-v2/apps/web`; using the monorepo root produces `Could not find a production build` even when `apps/web/.next/BUILD_ID` exists.
- Verified live: API `:4001` and web `:5501` online with zero PM2 restarts; local and public web/API health checks returned 200; Playwright rendered the production dashboard; `pm2 save` persisted the corrected command.
- Durability caveat: tracked `ecosystem.config.js` remains the legacy V1 definition. `/opt/erawanQPOS-v2/ecosystem.config.js` is a production-local modified V2 override, so future deployment tooling must preserve or formalize that distinction.

## 2026-09-22 — Erawan QPOS Early Stop Invariant, Group Checkout guestIndex & Sibling Refunds

- **Early Stop Invariant (enforced live, commit `8c2d5c9`)**: An early stop is an early completion, NOT a cancellation or refund. In `assignments.service.ts` `stop()`, non-cancel early stops set `status = 'completed'` with `endsAt = now`, preserve the therapist's commission (`CommissionEntry` retained), and transition the parent booking to `completed` so the sales figure is counted across all revenue reports, till settlement, and analytics. Only explicit cancellation/refund reasons set `status = 'cancelled'`.
- **Gantt Chart Exclusion Scope (commit `2fc2a76`)**: Only `cancelled` and `no_show` sessions, sessions whose payments are all `refunded`/`cancelled`, and parent bookings that are `cancelled`/`no_show` are removed from the Gantt chart. Stopped early and completed sessions always show.
- **Group Checkout `guestIndex` Preservation (commit `3b7e7dc`)**: In `payments.service.ts` `createGroupCheckout`, each payment must stamp `guestIndex: booking.items[0]?.guestIndex ?? 0` (never hardcoded 0), so each guest's line item and payment retain their respective guest indexes.
- **Group Invoice Detail Scope & Sibling Refunds (commit `3b7e7dc`)**: In `getDetail()`, when `payment.booking.groupId` and `receiptNo` exist, `isGuestScopedInvoice` is disabled so all sibling booking items sharing the tax invoice are included. In `refund()`, sibling payment lookup and assignment cancellation query across the shared `receiptNo`/`groupId` rather than only `payment.bookingId`.
- **SessionModifier Button Contract (commit `0f860fc`)**: In `SessionModifier`, the transaction button is strictly labeled `View TXN` (blue). Never show `Pay in POS` in the Gantt session block detail. Sibling payment fallback was removed so an unpaid session never displays someone else's transaction details.
